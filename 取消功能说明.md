# 取消处理功能说明

## 功能概述

系统现在支持在处理过程中取消任务，并自动清理GPU内存。这对于处理大图像或进度过慢的情况非常有用。

## 功能特点

### 1. 实时取消
- 在处理过程中可以随时点击"取消处理"按钮
- 取消请求会立即发送到后端
- 后端会停止当前处理并清理内存

### 2. 内存清理
- 取消时会自动清理GPU缓存
- 释放已分配但未使用的内存
- 确保资源正确释放

### 3. 用户反馈
- 取消按钮在处理时显示
- 显示"正在取消..."状态
- 完成后显示"处理已取消"提示

## 使用方法

### 前端操作

1. **开始处理**：点击"开始处理"按钮
2. **取消处理**：在处理过程中，点击"取消处理"按钮
3. **状态显示**：
   - 处理中：显示进度条和"处理中..."按钮
   - 取消中：显示"正在取消..."按钮（禁用状态）
   - 已取消：显示"已取消"消息

### 后端实现

后端通过以下机制实现取消：

1. **任务ID管理**：每个WebSocket连接分配唯一任务ID
2. **取消标志**：使用字典存储每个任务的取消状态
3. **检查点**：在处理的关键点检查取消标志
4. **异步监听**：后台任务监听取消消息

## 技术实现

### 后端实现

#### 1. 任务取消标志

```python
# 全局任务取消标志
task_cancelled = {}

# 为每个WebSocket连接创建任务ID
task_id = str(uuid.uuid4())
task_cancelled[task_id] = False

def check_cancelled():
    return task_cancelled.get(task_id, False)
```

#### 2. 取消消息监听

```python
async def receive_messages():
    while True:
        data = await websocket.receive_json()
        if data.get("type") == "cancel":
            task_cancelled[task_id] = True
            await websocket.send_json({
                "type": "cancelled",
                "message": "任务已取消"
            })
            return
```

#### 3. 处理过程中的检查点

```python
# 在关键处理点检查取消
if task_id and check_cancelled():
    raise asyncio.CancelledError("任务已取消")

# 在进度回调中检查
async def progress_callback(progress: int, message: str):
    if check_cancelled():
        raise asyncio.CancelledError("任务已取消")
    # ... 发送进度
```

#### 4. 取消时的清理

```python
except asyncio.CancelledError:
    # 任务被取消
    processing_service._clear_gpu_cache()
    await websocket.send_json({
        "type": "cancelled",
        "message": "处理已取消，内存已清理"
    })
```

### 前端实现

#### 1. WebSocket连接管理

```typescript
class ProcessingService {
  private currentWebSocket: WebSocket | null = null
  
  // 保存WebSocket引用
  this.currentWebSocket = ws
}
```

#### 2. 取消方法

```typescript
cancelProcessing(): boolean {
  if (this.currentWebSocket && 
      this.currentWebSocket.readyState === WebSocket.OPEN) {
    this.currentWebSocket.send(JSON.stringify({ type: 'cancel' }))
    return true
  }
  return false
}
```

#### 3. UI组件

```tsx
{uploading && (
  <Button
    danger
    icon={<StopOutlined />}
    onClick={handleCancel}
    loading={cancelling}
    disabled={cancelling}
    block
  >
    {cancelling ? '正在取消...' : '取消处理'}
  </Button>
)}
```

## 取消流程

```
用户点击取消按钮
    ↓
前端发送取消消息 (type: 'cancel')
    ↓
后端接收取消消息
    ↓
设置任务取消标志 (task_cancelled[task_id] = True)
    ↓
处理循环检查取消标志
    ↓
抛出 CancelledError
    ↓
捕获异常，清理GPU缓存
    ↓
发送取消确认消息 (type: 'cancelled')
    ↓
前端接收消息，更新UI
    ↓
关闭WebSocket连接
```

## 检查点位置

系统在以下位置检查取消标志：

1. **处理开始前**：解析图像后
2. **进度回调中**：每次更新进度时
3. **处理循环中**：每次重试前
4. **人脸增强前**：开始人脸增强处理前
5. **图像增强前**：开始图像超分辨率处理前
6. **处理完成后**：发送结果前

## 内存清理

取消时会执行以下清理操作：

1. **清理GPU缓存**：`torch.cuda.empty_cache()`
2. **同步GPU**：`torch.cuda.synchronize()`
3. **删除任务标志**：从 `task_cancelled` 字典中删除
4. **关闭连接**：关闭WebSocket连接

## 错误处理

### 取消失败的情况

如果取消失败（例如连接已断开），前端会：
- 显示警告消息："无法取消，连接可能已断开"
- 重置UI状态
- 允许用户重新开始

### 取消后的状态

取消后：
- 进度条保持当前进度（不重置）
- 显示"已取消"消息
- 可以重新开始处理
- GPU内存已清理

## 注意事项

1. **取消时机**：取消操作不是立即的，需要等待当前处理步骤完成
2. **内存释放**：取消后会立即清理GPU缓存，但已分配的内存可能需要一些时间才能完全释放
3. **连接状态**：如果WebSocket连接已断开，取消操作会失败
4. **重试机制**：如果正在重试（内存不足自动重试），取消会在重试完成后生效

## 使用建议

1. **大图像处理**：如果图像很大，处理时间较长，可以随时取消
2. **参数调整**：如果发现参数设置不当，可以取消后重新设置
3. **资源释放**：如果GPU内存不足，取消当前任务可以释放内存
4. **用户体验**：取消功能让用户有更多控制权，提升用户体验

## 总结

取消功能提供了：
- ✅ 实时取消处理任务
- ✅ 自动清理GPU内存
- ✅ 友好的用户反馈
- ✅ 安全的资源释放
- ✅ 良好的错误处理

这使得系统在处理大图像或进度过慢时更加灵活和用户友好。

