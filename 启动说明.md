# Real-ESRGAN 前端界面启动说明

## 环境要求

### 后端
- Python 3.7+
- PyTorch (已安装CUDA支持，如果有GPU)
- 已安装 Real-ESRGAN 项目依赖

### 前端
- Node.js 16+
- npm 或 yarn

## 启动步骤

### 1. 安装后端依赖

```bash
# 在项目根目录
pip install -r requirements.txt
pip install -r requirements_api.txt
```

### 2. 启动后端服务

```bash
# 在项目根目录
python api_server.py
```

后端服务将在 http://localhost:8000 启动

### 3. 安装前端依赖

```bash
# 进入前端目录
cd frontend
npm install
```

### 4. 启动前端开发服务器

```bash
# 在 frontend 目录
npm run dev
```

前端将在 http://localhost:3000 启动

### 5. 访问应用

在浏览器中打开 http://localhost:3000

## 使用说明

1. **上传图像**：点击或拖拽图像文件到上传区域
2. **配置参数**：在右侧面板调整处理参数
3. **开始处理**：点击"开始处理"按钮
4. **查看结果**：处理完成后在底部查看原图和修复后的对比

## 注意事项

- 确保已下载所需的模型文件到 `weights/` 目录
- 首次使用某个模型时，系统会自动下载（如果模型不存在）
- 大图像建议设置 Tile 大小以避免内存不足
- 如果遇到 CUDA 内存不足错误，请减小 Tile 大小或图像尺寸

