# 服务器优雅关闭和资源清理说明

## 功能概述

现在服务器支持优雅关闭，当使用 Ctrl+C 或其他方式停止服务器时，会自动清理所有资源，包括：
- ✅ 正在进行的任务
- ✅ GPU内存
- ✅ WebSocket连接
- ✅ 任务标志

## 关闭方式

### 1. Ctrl+C (SIGINT)
在终端中按 `Ctrl+C` 会触发优雅关闭：
```
^C
收到信号 2，开始优雅关闭...
正在清理资源...
清理GPU缓存...
GPU内存已清理
资源清理完成
```

### 2. 系统信号 (SIGTERM)
在Linux/Unix系统中，可以使用 `kill` 命令发送 SIGTERM 信号：
```bash
kill <pid>
```

### 3. 异常退出
如果服务器因为异常退出，也会自动清理资源。

## 清理流程

当收到关闭信号时，系统会按以下顺序清理资源：

### 1. 标记所有任务为取消
```python
for task_id in list(task_cancelled.keys()):
    task_cancelled[task_id] = True
```
- 所有正在进行的任务会被标记为取消
- 任务会在下一个检查点停止

### 2. 关闭所有WebSocket连接
```python
for ws in list(active_websockets):
    asyncio.create_task(ws.close())
```
- 关闭所有活跃的WebSocket连接
- 客户端会收到连接断开通知

### 3. 清理GPU缓存
```python
if torch.cuda.is_available():
    torch.cuda.empty_cache()
    torch.cuda.synchronize()
```
- 清空GPU缓存
- 同步GPU操作
- 释放已分配但未使用的GPU内存

### 4. 清理任务标志
```python
task_cancelled.clear()
active_websockets.clear()
```
- 清理所有任务标志
- 清理连接记录

## 检查点

系统在以下位置检查服务器关闭状态：

1. **处理开始前**：`enhance_image_local()` 开始时
2. **进度回调中**：每次更新进度时
3. **处理循环中**：每次重试前
4. **取消检查函数**：`check_cancelled()` 中

如果检测到服务器正在关闭，会立即抛出 `CancelledError` 并停止处理。

## 使用场景

### 场景1：正在处理大图像时关闭服务器

**情况**：服务器正在分块处理一张大图像，用户按 Ctrl+C

**处理流程**：
1. 收到 SIGINT 信号
2. 标记所有任务为取消
3. 当前处理块完成后停止
4. 清理GPU缓存
5. 关闭所有连接
6. 退出程序

**结果**：GPU内存被正确清理，不会留下未释放的内存。

### 场景2：多个客户端同时连接时关闭

**情况**：有多个客户端正在处理图像，服务器需要关闭

**处理流程**：
1. 收到关闭信号
2. 标记所有任务为取消
3. 关闭所有WebSocket连接
4. 等待当前处理完成或取消
5. 清理所有资源
6. 退出程序

**结果**：所有连接被正确关闭，资源被清理。

## 技术实现

### 信号处理

```python
def signal_handler(signum, frame):
    """信号处理器"""
    print(f"\n收到信号 {signum}，开始优雅关闭...")
    cleanup_resources()
    sys.exit(0)

# 注册信号处理器
signal.signal(signal.SIGINT, signal_handler)
signal.signal(signal.SIGTERM, signal_handler)  # Windows可能不支持
```

### 服务器关闭钩子

```python
async def shutdown_with_cleanup():
    print("\n服务器正在关闭...")
    cleanup_resources()
    await original_shutdown()

server.shutdown = shutdown_with_cleanup
```

### 关闭标志检查

```python
def check_cancelled():
    # 检查服务器是否正在关闭
    if server_shutting_down:
        return True
    return task_cancelled.get(task_id, False)
```

## 注意事项

### 1. Windows兼容性

- Windows 可能不支持 SIGTERM 信号
- SIGINT (Ctrl+C) 在所有平台都支持
- 信号处理在 Windows 上可能有限制

### 2. 异步操作

- WebSocket 关闭是异步操作
- 需要确保事件循环正在运行
- 如果事件循环未运行，会尝试创建新的事件循环

### 3. 清理时机

- GPU 缓存清理是同步的，会立即执行
- WebSocket 关闭是异步的，可能需要一些时间
- 任务取消会在下一个检查点生效

### 4. 模型缓存

- 默认不清理模型缓存（`upsampler_cache`）
- 清理模型缓存会释放已加载的模型
- 如果需要释放更多内存，可以取消注释相关代码

## 验证清理效果

### 方法1：查看控制台输出

关闭服务器时，应该看到：
```
收到信号 2，开始优雅关闭...
正在清理资源...
清理GPU缓存...
GPU内存已清理
资源清理完成
```

### 方法2：使用 nvidia-smi 监控

在另一个终端运行：
```bash
watch -n 1 nvidia-smi
```

然后按 Ctrl+C 关闭服务器，观察 GPU 内存是否被释放。

### 方法3：检查进程

关闭后检查进程是否完全退出：
```bash
ps aux | grep api_server
```

## 总结

现在服务器支持优雅关闭，确保：

✅ **资源清理**：GPU内存、连接、任务标志都被正确清理  
✅ **任务取消**：正在进行的任务会被标记为取消并停止  
✅ **连接关闭**：所有WebSocket连接会被正确关闭  
✅ **错误处理**：异常情况下也会清理资源  
✅ **跨平台支持**：Windows 和 Linux/Unix 都支持  

这确保了即使在使用 Ctrl+C 强制停止服务器时，GPU 内存也会被正确清理，不会留下未释放的资源。

