# Real-ESRGAN 云端部署指南

## 一、基础部署方案（单服务器）

### 1.1 最简单的部署方式

**是的，基础方案就是：将项目部署到云服务器，然后运行 `api_server.py` 即可。**

#### 部署步骤

1. **准备云服务器**
   - 选择有 GPU 的云服务器（推荐：NVIDIA GPU，至少 6GB 显存）
   - 操作系统：Ubuntu 20.04/22.04 或 CentOS 7/8
   - 确保已安装 Python 3.7+

2. **上传项目文件**
   ```bash
   # 方式1：使用 git
   git clone <your-repo-url>
   cd Real-ESRGAN
   
   # 方式2：使用 scp 上传
   scp -r /local/path/to/Real-ESRGAN user@server:/path/to/destination
   ```

3. **安装依赖**
   ```bash
   # SSH 连接到服务器
   ssh user@your-server-ip
   
   # 进入项目目录
   cd /path/to/Real-ESRGAN
   
   # 安装 Python 依赖
   pip install -r requirements.txt
   pip install -r requirements_api.txt
   
   # 如果使用 GPU，确保安装了 CUDA 版本的 PyTorch
   # 参考：https://pytorch.org/get-started/locally/
   ```

4. **下载模型文件（可选）**
   ```bash
   # 模型会在首次使用时自动下载，也可以提前下载
   mkdir -p weights
   # 下载需要的模型文件到 weights/ 目录
   ```

5. **启动服务**
   ```bash
   # 方式1：直接运行（开发/测试）
   python api_server.py
   
   # 方式2：后台运行（生产环境）
   nohup python api_server.py > server.log 2>&1 &
   
   # 方式3：使用 systemd（推荐生产环境）
   # 见下面的 systemd 配置
   ```

6. **配置防火墙**
   ```bash
   # 开放 8000 端口
   sudo ufw allow 8000
   # 或使用云服务器控制台的防火墙规则
   ```

7. **测试服务**
   ```bash
   # 在本地测试
   curl http://your-server-ip:8000/api/health
   ```

### 1.2 使用 systemd 管理服务（推荐）

创建 systemd 服务文件：

```bash
sudo nano /etc/systemd/system/realesrgan-api.service
```

内容：

```ini
[Unit]
Description=Real-ESRGAN API Server
After=network.target

[Service]
Type=simple
User=your-username
WorkingDirectory=/path/to/Real-ESRGAN
Environment="PATH=/usr/local/bin:/usr/bin:/bin"
ExecStart=/usr/bin/python3 /path/to/Real-ESRGAN/api_server.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

启用并启动服务：

```bash
sudo systemctl daemon-reload
sudo systemctl enable realesrgan-api
sudo systemctl start realesrgan-api
sudo systemctl status realesrgan-api
```

### 1.3 使用 Nginx 反向代理（可选）

如果需要使用域名和 HTTPS：

```nginx
# /etc/nginx/sites-available/realesrgan
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket 支持
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }
}
```

## 二、前端配置云端接口

### 2.1 修改前端配置

在前端的 `ProcessingService` 中，可以设置云端服务器地址：

```typescript
// frontend/src/services/ProcessingService.ts
const processingService = new ProcessingService('http://your-server-ip:8000')
// 或使用域名
// const processingService = new ProcessingService('https://your-domain.com')
```

### 2.2 切换处理模式

在 Header 组件中，用户可以切换"本地处理"和"云端处理"模式。

当切换到"云端处理"时：
- 前端会调用云服务器的 API
- 所有处理在云端 GPU 服务器上完成
- 结果通过网络返回

## 三、高级部署方案（多服务架构）

### 3.1 模块化部署

根据需求分析文档，可以将系统拆分为多个服务：

```
┌─────────────────┐
│   前端（客户端）   │
└────────┬────────┘
         │
         │ HTTP/WebSocket
         │
┌────────┴─────────────────────────┐
│         API 网关（可选）            │
│  - 负载均衡                        │
│  - 请求路由                        │
└────────┬─────────────────────────┘
         │
    ┌────┴────┬──────────┬──────────┐
    │         │          │          │
┌───▼───┐ ┌───▼───┐  ┌───▼───┐  ┌───▼───┐
│预处理 │ │推理   │  │后处理 │  │人脸   │
│服务   │ │服务   │  │服务   │  │增强   │
│       │ │(GPU)  │  │       │  │服务   │
└───────┘ └───────┘  └───────┘  └───────┘
```

### 3.2 Docker 容器化部署

#### 创建 Dockerfile

```dockerfile
# Dockerfile
FROM nvidia/cuda:11.8.0-runtime-ubuntu22.04

WORKDIR /app

# 安装 Python
RUN apt-get update && apt-get install -y \
    python3.10 \
    python3-pip \
    && rm -rf /var/lib/apt/lists/*

# 复制项目文件
COPY requirements.txt requirements_api.txt ./
RUN pip3 install --no-cache-dir -r requirements.txt -r requirements_api.txt

COPY . .

# 暴露端口
EXPOSE 8000

# 启动服务
CMD ["python3", "api_server.py"]
```

#### 使用 Docker Compose

```yaml
# docker-compose.yml
version: '3.8'

services:
  realesrgan-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./weights:/app/weights
      - ./inputs:/app/inputs
      - ./results:/app/results
    environment:
      - CUDA_VISIBLE_DEVICES=0
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped
```

启动：

```bash
docker-compose up -d
```

## 四、云端接口实现细节

### 4.1 当前实现状态

目前 `api_server.py` 中的 `enhance_image_cloud()` 方法是预留接口：

```python
async def enhance_image_cloud(
    self,
    image_data: bytes,
    params: EnhanceRequest,
    progress_callback=None
):
    """云端处理图像（预留接口）"""
    # TODO: 实现云端接口调用
    if not self.cloud_endpoints.get("inference"):
        raise HTTPException(status_code=500, detail="云端接口未配置")
    
    # 这里应该调用云端API
    raise NotImplementedError("云端处理功能待实现")
```

### 4.2 实现云端接口调用

如果需要调用其他云服务（如 AWS、Azure 等），可以这样实现：

```python
async def enhance_image_cloud(
    self,
    image_data: bytes,
    params: EnhanceRequest,
    progress_callback=None
):
    """云端处理图像"""
    import httpx
    
    if not self.cloud_endpoints.get("inference"):
        raise HTTPException(status_code=500, detail="云端接口未配置")
    
    inference_url = self.cloud_endpoints["inference"]
    
    # 准备请求数据
    files = {"file": ("image.jpg", image_data, "image/jpeg")}
    data = {
        "model_name": params.model_name,
        "scale": params.scale,
        "tile": params.tile,
        # ... 其他参数
    }
    
    # 调用云端API
    async with httpx.AsyncClient(timeout=300.0) as client:
        if progress_callback:
            await progress_callback(10, "正在上传图像到云端...")
        
        # 使用流式上传（如果支持）
        response = await client.post(
            f"{inference_url}/api/v1/enhance",
            files=files,
            data=data
        )
        
        if response.status_code != 200:
            raise HTTPException(
                status_code=response.status_code,
                detail=response.text
            )
        
        result = response.json()
        
        if progress_callback:
            await progress_callback(100, "云端处理完成")
        
        # 解码结果图像
        result_image_base64 = result.get("result_image", "").split(",")[-1]
        result_data = base64.b64decode(result_image_base64)
        
        return result_data, result.get("image_mode", "RGB")
```

### 4.3 配置云端接口地址

前端可以通过 API 配置云端接口：

```typescript
// 设置云端接口地址
processingService.setCloudEndpoints({
  preprocessing: 'http://preprocessing-server:8001',
  inference: 'http://inference-server:8002',
  postprocessing: 'http://postprocessing-server:8003',
  face_enhancement: 'http://face-server:8004'
})
```

后端 API：

```python
POST /api/v1/config/cloud-endpoints
{
  "preprocessing": "http://preprocessing-server:8001",
  "inference": "http://inference-server:8002",
  "postprocessing": "http://postprocessing-server:8003",
  "face_enhancement": "http://face-server:8004"
}
```

## 五、实际部署场景

### 场景1：单服务器部署（最简单）

**适用**：个人项目、小规模使用

```
客户端 → 云服务器（api_server.py）→ GPU处理
```

**步骤**：
1. 部署到一台有 GPU 的云服务器
2. 运行 `python api_server.py`
3. 前端配置服务器地址
4. 完成

### 场景2：多服务器部署（高性能）

**适用**：大规模使用、需要负载均衡

```
客户端 → 负载均衡器 → 多个推理服务器（GPU）
```

**步骤**：
1. 部署多个 `api_server.py` 实例到不同服务器
2. 使用 Nginx 或云负载均衡器
3. 配置负载均衡规则
4. 前端连接到负载均衡器地址

### 场景3：微服务架构（最灵活）

**适用**：大型项目、需要独立扩展

```
客户端 → API网关 → [预处理服务, 推理服务, 后处理服务, 人脸增强服务]
```

**步骤**：
1. 将不同功能拆分为独立服务
2. 每个服务独立部署
3. 使用服务发现和 API 网关
4. 前端通过网关调用

## 六、部署检查清单

### 6.1 服务器准备

- [ ] 云服务器已创建（有 GPU）
- [ ] 操作系统已安装（Ubuntu/CentOS）
- [ ] Python 3.7+ 已安装
- [ ] CUDA 和 cuDNN 已安装（如果使用 GPU）
- [ ] 防火墙端口已开放（8000）

### 6.2 项目部署

- [ ] 项目文件已上传到服务器
- [ ] 依赖已安装（requirements.txt）
- [ ] 模型文件已下载（或首次使用会自动下载）
- [ ] 目录权限已设置

### 6.3 服务启动

- [ ] 服务已启动（直接运行或 systemd）
- [ ] 服务可以访问（curl 测试）
- [ ] 日志正常输出
- [ ] GPU 可以正常使用

### 6.4 前端配置

- [ ] 前端已配置云端服务器地址
- [ ] 可以切换到"云端处理"模式
- [ ] 可以正常上传和处理图像
- [ ] 结果可以正常返回

## 七、常见问题

### Q1: 如何确认 GPU 可用？

```bash
# 检查 GPU
nvidia-smi

# 在 Python 中检查
python3 -c "import torch; print(torch.cuda.is_available())"
```

### Q2: 如何查看服务日志？

```bash
# 如果使用 systemd
sudo journalctl -u realesrgan-api -f

# 如果使用 nohup
tail -f server.log

# 如果使用 docker
docker-compose logs -f
```

### Q3: 如何重启服务？

```bash
# systemd
sudo systemctl restart realesrgan-api

# docker
docker-compose restart

# 直接运行
# 先 Ctrl+C 停止，再重新运行
```

### Q4: 如何处理多个 GPU？

```python
# 可以修改 api_server.py，支持多 GPU
# 使用 gpu_id 参数指定使用的 GPU
upsampler = RealESRGANer(..., gpu_id=0)  # 使用第一个 GPU
```

### Q5: 如何优化性能？

1. **使用 FP16**：默认已启用，可减少内存和加速
2. **调整 Tile 大小**：根据 GPU 内存调整
3. **使用更快的模型**：如 `realesr-animevideov3`
4. **启用模型缓存**：已实现，模型会缓存
5. **使用 CDN**：静态资源使用 CDN 加速

## 八、总结

### 最简单的部署方式

**是的，基础方案就是：**
1. 将项目部署到云服务器
2. 运行 `python api_server.py`
3. 前端配置服务器地址
4. 切换到"云端处理"模式

### 进阶部署

如果需要更高性能或更大规模：
- 使用 Docker 容器化
- 使用负载均衡
- 拆分为微服务架构

### 当前实现

- ✅ 单服务器部署：完全支持
- ✅ 前端切换：已实现
- ✅ 云端接口调用：预留接口，需要根据实际云服务实现
- ✅ 资源清理：已实现
- ✅ 错误处理：已实现

**推荐**：先使用单服务器部署验证功能，然后根据需求扩展到多服务器或微服务架构。

