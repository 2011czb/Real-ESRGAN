# 功能实现说明

## 一、FP16/FP32 精度模式

### 1.1 实现状态

✅ **已完全实现**

### 1.2 实现位置

在 `api_server.py` 的 `_create_upsampler()` 方法中：

```python
upsampler = RealESRGANer(
    scale=netscale,
    model_path=model_path,
    dni_weight=dni_weight,
    model=model,
    tile=params.tile,
    tile_pad=params.tile_pad,
    pre_pad=params.pre_pad,
    half=not params.fp32,  # FP16模式：half=True，FP32模式：half=False
    gpu_id=gpu_id
)
```

### 1.3 工作原理

- **FP16（半精度）**：`half=True`
  - 使用 16 位浮点数
  - 速度更快（约 1.5-2 倍）
  - 内存占用更少（约 50%）
  - 质量损失通常可忽略
  - **默认模式**

- **FP32（全精度）**：`half=False`
  - 使用 32 位浮点数
  - 速度较慢
  - 内存占用更多
  - 精度更高

### 1.4 使用方式

**前端参数面板**：
- 有一个开关："FP32 精度模式"
- 关闭（默认）：使用 FP16（快速）
- 开启：使用 FP32（精确）

**API 参数**：
```python
POST /api/v1/enhance
{
    "fp32": false  # false=FP16, true=FP32
}
```

### 1.5 性能对比

| 模式 | 速度 | 内存占用 | 质量 | 推荐场景 |
|------|------|----------|------|----------|
| FP16 | 快（1.5-2x） | 少（50%） | 高（几乎无损失） | 默认，大多数场景 |
| FP32 | 慢 | 多（100%） | 最高 | 需要最高精度时 |

## 二、人脸增强功能

### 2.1 实现状态

✅ **已完全实现**

### 2.2 实现位置

在 `api_server.py` 的 `enhance_image_local()` 方法中：

```python
if params.face_enhance:
    from gfpgan import GFPGANer
    face_enhancer = GFPGANer(
        model_path='https://github.com/TencentARC/GFPGAN/releases/download/v1.3.0/GFPGANv1.3.pth',
        upscale=params.outscale or params.scale,
        arch='clean',
        channel_multiplier=2,
        bg_upsampler=upsampler  # 使用Real-ESRGAN作为背景上采样器
    )
    _, _, output = face_enhancer.enhance(
        img, 
        has_aligned=False, 
        only_center_face=False, 
        paste_back=True
    )
```

### 2.3 工作原理

- 使用 **GFPGAN** 进行人脸增强
- Real-ESRGAN 处理整个图像（包括背景）
- GFPGAN 专门增强人脸区域
- 最后将增强的人脸粘贴回原图

### 2.4 使用方式

**前端参数面板**：
- 有一个开关："人脸增强 (GFPGAN)"
- 关闭（默认）：只进行图像超分辨率
- 开启：同时进行人脸增强

**API 参数**：
```python
POST /api/v1/enhance
{
    "face_enhance": true  # 启用人脸增强
}
```

### 2.5 注意事项

1. **模型下载**：首次使用时会自动下载 GFPGAN 模型（约 300MB）
2. **处理时间**：启用人脸增强会增加处理时间（约 20-30%）
3. **适用场景**：
   - ✅ 适合：人物照片、肖像
   - ❌ 不适合：动漫图像、风景照片
4. **模型兼容性**：某些模型（如动漫模型）可能不支持人脸增强

### 2.6 依赖要求

需要安装 `gfpgan` 包：

```bash
pip install gfpgan
```

已在 `requirements.txt` 中包含。

## 三、WebSocket 错误修复

### 3.1 错误原因

错误信息：`cannot call recv while another coroutine is already waiting for the next message`

**原因**：
- `receive_messages()` 任务在循环中等待 `websocket.receive_json()`
- 主流程也在等待 `websocket.receive_json()` 接收初始请求
- 两个协程同时等待同一个 WebSocket 的消息，导致冲突

### 3.2 修复方案

**修复前**：
```python
# 错误：先启动接收任务，再接收初始请求
receive_task = asyncio.create_task(receive_messages())  # 开始等待消息
data = await websocket.receive_json()  # 冲突！
```

**修复后**：
```python
# 正确：先接收初始请求，再启动接收任务
data = await websocket.receive_json()  # 先接收初始请求
receive_task = asyncio.create_task(receive_messages())  # 然后监听取消消息

# 并且使用 receive_text 而不是 receive_json，避免冲突
message = await websocket.receive_text()
data = json.loads(message)
```

### 3.3 修复内容

1. **调整接收顺序**：先接收初始请求，再启动监听任务
2. **使用 receive_text**：监听任务使用 `receive_text()` 而不是 `receive_json()`，避免冲突
3. **错误处理**：改进异常处理，正确处理连接断开

### 3.4 修复后的流程

```
1. WebSocket 连接建立
2. 接收初始请求（type: 'start'）
3. 启动监听任务（监听取消消息）
4. 开始处理图像
5. 监听任务在后台运行，等待取消消息
6. 处理完成或收到取消消息
7. 清理资源
```

## 四、功能验证

### 4.1 验证 FP16/FP32

```bash
# 测试 FP16（默认）
curl -X POST http://localhost:8000/api/v1/enhance \
  -F "file=@test.jpg" \
  -F "fp32=false"

# 测试 FP32
curl -X POST http://localhost:8000/api/v1/enhance \
  -F "file=@test.jpg" \
  -F "fp32=true"
```

### 4.2 验证人脸增强

```bash
# 测试人脸增强
curl -X POST http://localhost:8000/api/v1/enhance \
  -F "file=@portrait.jpg" \
  -F "face_enhance=true"
```

### 4.3 验证 WebSocket

使用前端界面：
1. 上传图像
2. 点击"开始处理"
3. 观察进度更新
4. 点击"取消处理"（测试取消功能）
5. 应该不再出现 WebSocket 错误

## 五、总结

### 已实现功能

✅ **FP16/FP32 精度模式**
- 完全实现
- 前端可切换
- 默认使用 FP16（快速）

✅ **人脸增强功能**
- 完全实现
- 使用 GFPGAN
- 前端可开关
- 自动下载模型

✅ **WebSocket 错误修复**
- 修复了并发接收消息的冲突
- 改进了错误处理
- 支持取消功能

### 使用建议

1. **FP16 vs FP32**：
   - 大多数情况使用 FP16（默认）
   - 需要最高精度时使用 FP32

2. **人脸增强**：
   - 人物照片建议开启
   - 动漫/风景照片建议关闭
   - 会增加处理时间

3. **WebSocket**：
   - 现在可以正常使用实时进度
   - 可以正常取消处理
   - 不会出现并发错误

