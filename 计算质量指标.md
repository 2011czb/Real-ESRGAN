# 图像质量指标计算指南

本指南介绍如何使用 `compute_image_metrics.py` 为处理前/后的图像计算 PSNR、SSIM、LPIPS，并将结果导出为表格。

## 指标含义

- **PSNR (Peak Signal-to-Noise Ratio)**：衡量重建图像与参考图像之间的像素级误差，值越大越接近原图。对强平滑、轻微错位较敏感。
- **SSIM (Structural Similarity)**：从亮度、对比度、结构三个维度评估两张图的结构一致性，范围通常 0~1，越接近 1 越好。
- **LPIPS (Learned Perceptual Image Patch Similarity)**：利用深度网络评估两张图的感知距离，值越小说明越相似，比 PSNR/SSIM 更贴近人眼主观感受。

这三个指标都需要“参考图像”才能计算（LPIPS 也需要成对图像），因此**必须**在处理前/后间进行对比，才能得到有意义的质量数值。

## 环境准备

```bash
pip install -r requirements.txt
```

> 若仅使用此脚本，也可单独安装 `scikit-image` 与 `lpips`。
pip install scikit-image
pip install lpips

## 使用方式

### 1. 批量对比两个文件夹（对比的是图像的文件名相同的图像，所以要确保图像的文件名相同）

将处理前的图像放在 `inputs/before`，处理后放在 `results/after`，文件名保持一致：

```bash
python compute_image_metrics.py \
  --before inputs/before \
  --after results/after \
  --output results/image_metrics.csv \
  --metrics psnr ssim lpips \
  --device auto
```
一行命令，效果同上：
```bash
python compute_image_metrics.py --before inputs/before --after results/after --output results/image_metrics.csv --metrics psnr ssim lpips --device auto --max-size 4096
```

**常用参数说明：**
- `--recursive`：递归遍历子目录。
- `--extensions`：限定扫描的后缀。
- `--device`：LPIPS 计算的设备（`auto` 会优先用 GPU）。
- `--max-size`：最大图像尺寸（宽度或高度），超过此尺寸的图像会被下采样以加速计算。详见下方说明。
- 分辨率不一致时，脚本会自动将两张图缩放到像素更多（通常是修复后）的分辨率再计算指标。

### 2. 单张图像

```bash
python compute_image_metrics.py \
  --before inputs/sample.png \
  --after results/sample.png \
  --output results/sample_metrics.csv
```

一行命令，效果同上：
```bash
python compute_image_metrics.py --before inputs/sample.png --after results/sample.png --output results/sample_metrics.csv
```

如果图像分辨率很大，可以添加 `--max-size` 参数加速计算：
```bash
python compute_image_metrics.py --before inputs/sample.png --after results/sample.png --output results/sample_metrics.csv --max-size 4096
```

## 输出结果

脚本会创建一个 CSV：（在results文件夹中）

```
image,psnr,ssim,lpips
sample1.png,28.51,0.842,0.092
sample2.png,30.12,0.873,0.081

metric,value
avg_psnr,29.315
avg_ssim,0.8575
avg_lpips,0.0865
```

- 第一段列出每张图片的指标。
- 第二段给出各指标的平均值，可用于整体质量评估。

## 大分辨率图像优化（`--max-size` 参数）

对于超大分辨率图像（如 4K、8K 或更高），计算指标可能会非常耗时，特别是 LPIPS 指标。使用 `--max-size` 参数可以自动下采样图像以加速计算。

### 参数说明

`--max-size` 指定图像的最大尺寸（宽度或高度），超过此尺寸的图像会按比例下采样，保持宽高比。

### 不同值的影响

| 值 | 速度 | PSNR/SSIM 影响 | LPIPS 影响 | 适用场景 |
|---|---|---|---|---|
| 不使用 | 很慢（可能数小时） | 无影响 | 无影响 | 需要最高精度 |
| `4096` | 中等（约 6 倍加速） | 几乎无影响（< 2%） | 无影响 | 高精度需求 |
| `2048` | 较快（约 25 倍加速） | 影响较小（< 5%） | 几乎无影响 | **推荐，平衡速度与精度** |
| `1024` | 快（约 100 倍加速） | 可能偏差较大（5-15%） | 影响很小（< 3%） | 快速评估、仅关注 LPIPS |
| `512` | 最快 | 偏差较大 | 影响较小 | 仅用于快速测试 |

### 使用建议（推荐用3）

1. **首次使用**：建议先用 `--max-size 2048` 测试，如果速度可接受且结果合理，就使用这个值。
2. **仅计算 LPIPS**：如果只关注感知质量（LPIPS），可以使用 `--max-size 1024`，因为 LPIPS 是感知指标，对分辨率相对不敏感。
3. **需要高精度**：如果 PSNR/SSIM 的准确性很重要，建议使用 `--max-size 4096` 或更大，甚至不使用该参数。
4. **批量处理**：对于大量图像，建议使用 `--max-size 2048` 以平衡速度和精度。

### 示例

```bash
# 推荐：平衡速度与精度
python compute_image_metrics.py --before inputs/before --after results/after --max-size 2048

# 快速评估（仅关注 LPIPS 时）
python compute_image_metrics.py --before inputs/before --after results/after --max-size 1024

# 高精度（需要准确的 PSNR/SSIM）
python compute_image_metrics.py --before inputs/before --after results/after --max-size 4096
```

**注意**：下采样会同时应用于参考图像和处理后图像，因此指标的可比性仍然保持。脚本会在下采样时输出提示信息。

## 建议

1. **指标互补**：PSNR/SSIM 反映像素与结构相似，LPIPS 反映感知质量，综合观察更可靠。
2. **对齐尺寸**：脚本会自动缩放到较大的分辨率，但若希望严格对齐，可在运行前手动统一尺寸。
3. **质量解释**：处理后若 PSNR、SSIM 提升而 LPIPS 降低（变小），表示客观与感知质量都提升；若指标分歧，可结合人工主观评估。
4. **大图像处理**：对于超大分辨率图像（> 4K），建议使用 `--max-size` 参数以加速计算，特别是计算 LPIPS 时。

