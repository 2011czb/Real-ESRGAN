# Requirements for torch 2.5.1+cu121
# 保持你现有的GPU版本

# 使用你已有的torch版本
# torch==2.5.1+cu121 (已安装)
# torchvision==0.20.1+cu121 (已安装) 
# torchaudio==2.5.1+cu121 (已安装)

# 其他依赖
basicsr>=1.4.2
facexlib>=0.2.5
gfpgan>=1.3.5
numpy<2.0
opencv-python
Pillow
tqdm
typing_extensions

