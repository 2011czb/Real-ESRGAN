# GPU/CPU 使用和内存优化说明

## 一、设备选择机制

### 1.1 自动设备检测

系统会自动检测并使用可用的设备：

- **如果有GPU（CUDA可用）**：自动使用GPU进行加速
- **如果没有GPU**：自动回退到CPU处理

**实现位置**：`api_server.py` 的 `_create_upsampler()` 方法

```python
use_gpu = torch.cuda.is_available()
gpu_id = 0 if use_gpu else None
```

### 1.2 设备信息查询

可以通过健康检查接口查看当前使用的设备：

```bash
GET /api/health
```

返回示例：
```json
{
  "status": "healthy",
  "mode": "local",
  "device": {
    "device": "GPU",
    "device_name": "NVIDIA GeForce RTX 3060",
    "memory_total_gb": 12.0,
    "memory_allocated_gb": 2.5,
    "memory_free_gb": 9.5
  }
}
```

## 二、内存优化机制

### 2.1 自动分块处理（Auto Tile）

系统会根据以下条件自动启用分块处理：

1. **图像尺寸**：如果图像任意边超过2048像素
2. **估算内存**：如果估算的所需内存超过GPU可用内存的60%
3. **GPU内存大小**：根据GPU可用内存自动选择tile大小
   - < 2GB：tile = 200
   - < 4GB：tile = 400
   - < 8GB：tile = 600
   - ≥ 8GB：tile = 800

**实现位置**：`_calculate_auto_tile()` 方法

### 2.2 内存不足自动重试

当遇到 CUDA 内存不足错误时，系统会：

1. **自动清理GPU缓存**
2. **减小tile大小**（每次减少200）
3. **重新创建upsampler**
4. **自动重试处理**（最多重试2次）

如果重试后仍然失败，会返回友好的错误提示，建议用户：
- 减小图像尺寸
- 手动设置tile参数（建议400-800）
- 关闭其他占用GPU的程序

**实现位置**：`enhance_image_local()` 方法的错误处理部分

### 2.3 GPU缓存清理

在处理前后自动清理GPU缓存，释放未使用的内存：

```python
def _clear_gpu_cache(self):
    if torch.cuda.is_available():
        torch.cuda.empty_cache()
        torch.cuda.synchronize()
```

**调用时机**：
- 处理开始前
- 处理完成后
- 内存不足重试前
- 发生错误时

## 三、使用建议

### 3.1 大图像处理

对于大图像（> 2048像素），建议：

1. **让系统自动处理**：不设置tile参数（或设置为0），系统会自动启用分块
2. **手动设置tile**：如果知道图像很大，可以提前设置tile=400或800
3. **监控GPU内存**：通过 `/api/health` 接口查看GPU内存使用情况

### 3.2 内存不足时的处理

如果遇到内存不足错误：

1. **减小图像尺寸**：在上传前先压缩图像
2. **设置tile参数**：在参数面板中设置tile=400或更小
3. **使用FP16模式**：默认已启用，可减少约50%内存占用
4. **关闭其他程序**：释放GPU内存

### 3.3 性能优化建议

- **小图像（< 1024像素）**：tile=0（不分块），速度最快
- **中等图像（1024-2048像素）**：tile=400，平衡速度和内存
- **大图像（> 2048像素）**：tile=600-800，避免内存不足

## 四、技术细节

### 4.1 内存估算

系统使用简化的内存估算公式：

```
估算内存(MB) = (输出高度 × 输出宽度 × 4字节) / (1024 × 1024)
```

其中：
- 输出高度 = 输入高度 × 缩放比例
- 输出宽度 = 输入宽度 × 缩放比例
- 4字节 = 假设每个像素4字节（RGBA或浮点数）

### 4.2 Tile处理原理

Tile处理将大图像分割成小块处理：

1. 将图像分割成多个tile（例如400×400像素）
2. 每个tile独立处理
3. 处理时添加padding避免边界伪影
4. 最后合并所有tile得到完整结果

**优点**：
- 减少单次处理的内存占用
- 可以处理任意大小的图像

**缺点**：
- 处理时间略有增加（约10-20%）
- 可能存在轻微的边界伪影（通过padding缓解）

### 4.3 模型缓存

系统会缓存已加载的模型，避免重复加载：

- **缓存键**：`{model_name}_{tile}_{fp32}`
- **缓存位置**：`upsampler_cache` 全局字典
- **缓存策略**：不同tile大小的模型会分别缓存

**注意**：如果切换tile大小，会创建新的模型实例并缓存。

## 五、错误处理

### 5.1 CUDA内存不足错误

系统会捕获以下错误并自动处理：

```
CUDA out of memory. Tried to allocate X GiB. GPU 0 has a total capacity of Y GiB...
```

**处理流程**：
1. 捕获RuntimeError
2. 检查错误信息是否包含"CUDA out of memory"
3. 清理GPU缓存
4. 减小tile大小
5. 重新创建模型
6. 重试处理

### 5.2 其他错误

其他类型的错误会直接返回给用户，包含详细的错误信息。

## 六、监控和调试

### 6.1 查看设备信息

```bash
curl http://localhost:8000/api/health
```

### 6.2 查看处理进度

通过WebSocket接口可以实时查看：
- 使用的设备（GPU/CPU）
- GPU内存使用情况
- 是否启用了自动tile
- 处理进度和状态

### 6.3 日志输出

后端会在控制台输出：
- 设备选择信息
- 内存使用情况
- 错误信息
- 重试信息

## 七、总结

系统现在具备以下特性：

✅ **自动设备选择**：GPU优先，CPU回退  
✅ **智能内存管理**：自动分块、自动重试  
✅ **实时监控**：设备信息、内存使用  
✅ **友好错误提示**：内存不足时的处理建议  
✅ **性能优化**：GPU缓存清理、模型缓存  

这些改进使得系统能够：
- 自动处理各种大小的图像
- 在内存不足时自动调整策略
- 提供清晰的错误提示和处理建议
- 最大化利用可用硬件资源

